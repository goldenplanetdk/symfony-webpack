imports: [{ resource: 'common.yml' }]

golden_planet_webpack:

    enabled_bundles:
        - TestWebpackBundle
        - TestWebpackAnotherBundle

    twig:
        suppress_errors: false

    config:
        parameters:
            extractCss: false
