imports:
    - { resource: monolog_console.yml }
#    - { resource: monolog_file.yml }

framework:

    profiler: ~

    router:
        resource: '%kernel.root_dir%/config/routing.yml'
        strict_requirements: ~

    secret: 'secret'

    templating:
        engines: ['twig', 'php']

    translator: ~

golden_planet_webpack:
    config:
        parameters:
            logConfig: true
