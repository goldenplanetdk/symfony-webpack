imports: [{ resource: 'common.yml' }]

golden_planet_webpack:

    enabled_bundles:
        - TestWebpackBundle

    twig:
        suppress_errors: ignore_unknowns
        additional_directories:
            - '%kernel.root_dir%/Resources/views-parse-error'
